# box_kite_engine — the established object, and this paper's own reading of it

Copied from `ValaQuenta/`, 2026-09-21.

- **`maths.py`**, **`tools.py`** — `ValaQuenta/modules/box_kite/`, this
  project's root authoritative engine code: `pencil(s)`, `box_kites()`,
  `assessors()`, `chart_of()` (§9.2, §13.5).
- **`box_kite.md`** — the established box-kite object this paper builds
  on but does not re-derive (de Marrais 2000; §9, §12).
- **`pencil_hyperstring.md`** — the `Φ_w` deformation-law conjecture
  §9.3–§9.4 test against, including the acceptance-test wording that
  led to both of this paper's own corrected mistakes (Cartesian frame,
  quantized aperture).
- **`inversion.md`** — the formal `J_N` definition (order-4,
  `(r,θ)→(1/r,θ+π/2)`) §9.5 distinguishes the crossing's real symmetry,
  `J_2`, from.
