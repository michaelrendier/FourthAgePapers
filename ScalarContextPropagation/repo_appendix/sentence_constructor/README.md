# sentence_constructor — the live sentence-construction path, §11

Copied from `VAPMIP/`, 2026-09-21. None of this paper's windspeed feeds
it yet (§11) — included in full because it's the real, running system
a reader who goes looking for "what happens after the address" will
find.

- **`rotary_rerun_boxkite_monad.py`** — the real, live `BoxKite` object
  (`ValaQuenta.modules.box_kite`, genuinely 7 struts / 42 Assessors),
  built via `BoxKite.between(eye, hands)`, *"the relational language
  spoken by both MindsEye and PapersHands"* — signed so a third party
  can verify which two subsystems co-authored it.
- **`rotary_rerun_monad.py`** — the wider monad this object is built
  inside of.
- **`grammar/`** — the real sentence creator in full: VerbNet sails
  (`sails.py`), WordNet hypernym-closure fill (`ptol_relations.py`,
  `frames.py`), the SELRESTR gate (`generate.py`), the sedenion-window
  grammar-position mapping (`slot_shells.py`), the semantic hash
  (`sem_hash.py`), CoNLL-U handling (`conllu.py`), maths sanitization
  for spoken output (`maths_sanitize.py`), the build step (`build.py`),
  the shadow/debug path (`shadow.py`), and the install point,
  `rotary_bridge.py` — confirmed live 2026-09-11, running end to end
  this session (`voice:creator` in a real transcript).
